apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: 
  name: notes
spec:
  ingressClassName: nginx
  rules:
    - host: notes.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: notes
                port:
                  number: 8080
